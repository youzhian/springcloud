server:
  port: 8480
spring:
  application:
    name: provide-server
eureka:
  hostname: localhost
  port: 8280
  client:
#    register-with-eureka: false
#    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.hostname}:${eureka.port}/eureka/